<?xml version="1.0" encoding="utf-8"?>
<!-- Debug-only network security configuration.

     Allows cleartext HTTP to the host machine's loopback aliases so a debug
     build can reach the local backend from:
       - the Android emulator via 10.0.2.2 (alias for the host's 127.0.0.1), and
       - a device/emulator via `adb reverse` using localhost / 127.0.0.1.

     Release builds do not reference this file and keep the platform default
     (cleartext blocked). -->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
